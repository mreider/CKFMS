groups:
  - id: tibco
    type: resource
    brief:
    attributes:
      - id: tibco.businessworks_ce.app.name
        type: string
        stability: experimental # traces-in-grail
        brief:
        examples: [ ' ' ]

      - id: tibco.businessworks_ce.version
        type: string
        stability: experimental # traces-in-grail
        brief:
        examples: [ ' ' ]

      - id: tibco.businessworks.app.node.name
        type: string
        stability: experimental # traces-in-grail
        brief:
        examples: [ ' ' ]

      - id: tibco.businessworks.app.space.name
        type: string
        stability: experimental # traces-in-grail
        brief:
        examples: [ ' ' ]

      - id: tibco.businessworks.domain.name
        type: string
        stability: experimental # traces-in-grail
        brief:
        examples: [ ' ' ]

      - id: tibco.businessworks.home
        type: string
        stability: experimental # traces-in-grail
        brief:
        examples: [ ' ' ]

      - id: tibco.businessworks.property.file.path
        type: string
        stability: experimental # traces-in-grail
        brief:
        examples: [ ' ' ]

      - id: tibco.businessworks.property.file.name
        type: string
        stability: experimental # traces-in-grail
        brief:
        examples: [ ' ' ]
